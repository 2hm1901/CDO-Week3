# Pod này có label app nên thỏa policy Gatekeeper và được admit.
apiVersion: v1
kind: Pod
metadata:
  name: with-label
  namespace: dev
  labels:
    app: nginx
spec:
  containers:
    - name: nginx
      image: nginx:1.27-alpine
