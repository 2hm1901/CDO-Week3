# Pod này cố tình không có metadata.labels.app.
# Khi policy ở deny mode, Gatekeeper sẽ reject manifest này.
apiVersion: v1
kind: Pod
metadata:
  name: missing-label
  namespace: dev
spec:
  containers:
    - name: nginx
      image: nginx:1.27-alpine
